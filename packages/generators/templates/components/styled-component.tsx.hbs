import { ComponentProps, ElementType } from 'react'

import { styled } from '../styles'

export const {{pascalCase name}} = styled('div', {})

export interface {{pascalCase name}}Props extends ComponentProps<typeof {{pascalCase name}}> {
  as?: ElementType
}
